<UserControl x:Class="WpfPuzzleApp.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfPuzzleApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
      

    </UserControl.Resources>

    <UserControl.Background>
        <ImageBrush ImageSource="/Views/Photo/mainWindow.jpg"/>
    </UserControl.Background>

    <UniformGrid  HorizontalAlignment="Left" Width="200" Height="200" Columns="0" Rows="4"  >
        <Button Height="50" Command="{Binding PlayCommand}" >Play</Button>
        <Button Height="50" Command="{Binding NewGameCommand}" >New Game</Button>
        <Button Height="50" Command="{Binding SettingCommand}">Settings</Button>
        <Button Height="50" Command="{Binding ExitCommand}">Exit</Button>
        
    </UniformGrid>
</UserControl>
